
<div id="right-sidebar"> 
	<div class="innerdiv"> 
		<h2 class="head">Controles</h2> 
		<div class="innercontent clear"> 
			<h4>Recentes</h4> 
			<ul id="comments"> 
				<% @controls.limit(3).each do |controle| %>
				<li>
					<%= image_tag "icons/database.png"%>
					<span class="sender"><%= link_to controle.nome, controle %></span> 
					<span class="description"><%= controle.descricao %></span> 
					<span class="date"><%= distance_of_time_in_words(controle.created_at, Time.now) %></span> </a> 
					<br class="clear"/> 
				</li> 
			<% end %>
				<li class="last">
					<%= link_to controls_path, :class=>"all-messages" do %>
						Veja todos os controles<span class="unreaded">Total de <%= pluralize(@controls.count, "controle", "controles") %>.</span>
						<% end %>
				</li> 
			</ul> 
		</div> 
	</div> 
</div>